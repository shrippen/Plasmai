[pytest]
testpaths = tests
pythonpath = tests
markers =
    viewer: launches plasmoidviewer (needs DISPLAY and plasma-sdk)
    shell: optional click sweep against a running plasmashell
filterwarnings =
    ignore::DeprecationWarning
